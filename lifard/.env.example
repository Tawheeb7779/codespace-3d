# Optional. Without it the enquiry form opens the visitor's mail client with
# the message already written — the site works fully with no backend.
# With it, the form POSTs the enquiry as JSON to this URL instead.
VITE_INQUIRY_ENDPOINT=
